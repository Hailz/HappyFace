<h1><img class="vanityPageIcons" src="../img/nailpolish.png"> Nailpolish</h1>

<% if (product.length > 0){ %>
 <div class="box row">
    <% product.forEach(function(product){ %>
      <% include ../partials/favorite.ejs %>
    <% }) %>
  </div>

<% } else{ %>
  <h3>You don't have any saved products of this type!</h3>
<% } %>