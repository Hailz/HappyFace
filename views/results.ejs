<h1>Results</h1>

<% if (results.length > 0){ %>
  <% results.forEach(function(result){ %>
    <% include ./partials/result.ejs %>
  <% }); %>
<% } else{ %>
  <h3>Sorry, no products currently match your search terms.</h3>
<% } %>